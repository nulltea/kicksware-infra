# install: helm upgrade --install -n toolstack apm-server elastic/apm-server -f=elasticstack/apm-server.yaml

fullnameOverride: apm-server

resources:
  requests:
    cpu: "100m"
    memory: "50Mi"
  limits:
    cpu: "250m"
    memory: "100Mi"